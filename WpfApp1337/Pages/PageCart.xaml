<Page x:Class="WpfApp1337.Pages.PageCart"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Корзина" Background="#F4F6F8">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- Шапка -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid Margin="16,0">
                <TextBlock Text="🛒 Корзина" VerticalAlignment="Center"
                           FontSize="18" FontWeight="Bold" Foreground="#1A237E"/>
                <Button Content="← Назад" Width="90" Height="32"
                        HorizontalAlignment="Right" Click="OnBackClick"/>
            </Grid>
        </Border>

        <!-- Список товаров в корзине -->
        <DataGrid x:Name="CartGrid" Grid.Row="1" Margin="16,10,16,4"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Товар"    Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Цена, ₽"
                                    Binding="{Binding UnitPrice, StringFormat=\{0:N0\}}" Width="120"/>
                <DataGridTextColumn Header="Кол-во"   Binding="{Binding Quantity}"    Width="80"/>
                <DataGridTextColumn Header="Сумма, ₽"
                                    Binding="{Binding TotalPrice, StringFormat=\{0:N0\}}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid Margin="16,0">
                <TextBlock x:Name="TotalBlock" VerticalAlignment="Center"
                           FontSize="16" FontWeight="Bold" Foreground="#1A237E"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <Button Content="🗑 Удалить позицию" Width="140" Height="34"
                            Margin="0,0,10,0" Click="OnRemoveClick"/>
                    <Button Content="✅ Оформить заказ" Width="150" Height="34"
                            Background="#388E3C" Foreground="White"
                            Click="OnCheckoutClick"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
