<Page x:Class="WpfApp1337.Pages.Autorization"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="White">
    <Grid Background="White">
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border Background="White" CornerRadius="8" Padding="20" Width="380" Height="280">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtLogin" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Height="28"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                    <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Height="28"/>

                    <Button x:Name="btnVhod" Grid.Row="2" Grid.ColumnSpan="2" Content="Войти" Height="30" Width="100" Margin="0,10,0,0" HorizontalAlignment="Center"
                    Click="btnVhod_Click"/>

                    <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <TextBlock VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12">Нет аккаунта?</TextBlock>
                        <Button x:Name="btnReg" Content="Зарегистрироваться" Click="btnReg_Click" Height="26" FontSize="12"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>