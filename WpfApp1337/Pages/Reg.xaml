<Page x:Class="WpfApp1337.Pages.Reg"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      MinWidth="480" MinHeight="380" Background="#F4F6F8">
    <Grid Margin="16">
        <Border Background="White" CornerRadius="8" Padding="20" MinWidth="480" MinHeight="380">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Имя:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                    <TextBox x:Name="FullNameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Логин:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                    <TextBox x:Name="LoginTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Пароль:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                    <PasswordBox x:Name="PasswordBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" 
                                 PasswordChanged="PasswordBoxes_PasswordChanged"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Повторите пароль:" Margin="0,0,8,8" VerticalAlignment="Center"/>
                    <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" 
                                 PasswordChanged="PasswordBoxes_PasswordChanged"/>

                    <Button x:Name="RegisterButton" Grid.Row="4" Grid.ColumnSpan="2" 
                            Content="Зарегистрировать" Height="32" Margin="0,10,0,0" 
                            Click="OnRegisterClick"/>

                    <Button x:Name="BackButton" Grid.Row="5" Grid.ColumnSpan="2" 
                            Content="Вернуться" Height="32" Margin="0,5,0,0" 
                            Click="BackButton_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>